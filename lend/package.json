{
  "name": "ribbon-lend",
  "license": "UNLICENSED",
  "scripts": {
    "cb": "graph codegen && graph build",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy-studio": "graph deploy --studio ribbonlend",
    "deploy-test": "graph deploy --studio ribbonlendtest"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.21.0",
    "@graphprotocol/graph-ts": "^0.20.0"
  }
}
